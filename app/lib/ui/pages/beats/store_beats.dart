import 'package:flutter/material.dart';

class StoreBeats extends StatelessWidget {
  const StoreBeats({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Coming soon!"));
  }
}